# Subs whose failures are tracked and shown on the dashboard but do NOT
# generate alert DMs.
#
# Entries may be either:
#   a whole sub          e.g.  somesub
#   a single event stream e.g. somesub:modmail
#
# The second form lets you silence one noisy signal while still being
# alerted about that sub's other failures.
#
# NOTE: a sub that removes the bot is auto-pruned from trusted_subs.txt,
# so it stops being polled entirely — no mute needed. Don't leave
# departed subs listed here, or they'd be silently muted if they rejoin.
