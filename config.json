{
  "CROSS_SUB_BAN_REASON": "Auto XSub Pact Ban",
  "EXEMPT_USERS": [
    "AutoModerator",
    "xsub-pact-bot"
  ],
  "DAILY_BAN_LIMIT": 30,
  "MAX_LOG_AGE_MINUTES": 60,
  "_comment_retention": "Ban rows are the only link between Reddit-side bans and the pact. Deleting them breaks late unbans/pardons, forgiveness memory, exemptions, and /xsub history. 3650 days = effectively permanent.",
  "ROW_RETENTION_DAYS": 3650,
  "_comment_sqlite": "Set USE_SQLITE to true to enable high-performance SQLite backend",
  "USE_SQLITE": true,
  "SQLITE_DB_PATH": "bans.db",
  "PUBLIC_LOG_RETENTION_DAYS": 30
}
